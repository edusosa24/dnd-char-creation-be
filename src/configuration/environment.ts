const PORT = 3000;
const SECRET = process.env.SECRET || '';
const ADMIN_USER = process.env.ADMIN_USER || '';
const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || '';

export default { PORT, SECRET, ADMIN_USER, ADMIN_PASSWORD };
